[Unit]
Description=starts mitmproxy and configure dns
After=network.target
StartLimitIntervalSec=0

[Service]
Type=simple
Restart=always
RestartSec=1
User=root
ExecStart=dhclient && /usr/bin/mitmdump

[Install]
WantedBy=multi-user.target
